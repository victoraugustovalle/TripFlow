VITE_API_BASE_URL=https://localhost:7194
# Opcional - so aparece o botao "Entrar com Google" se isso estiver preenchido (client ID OAuth
# do Google Cloud, o mesmo configurado em GoogleAuth:ClientId no backend).
VITE_GOOGLE_CLIENT_ID=

namespace TripFlow.Domain.Enums;

/// <summary>Papel do participante dentro de UMA viagem especifica (nao confundir com GlobalRole).</summary>
public enum TripRole
{
    Viewer = 0,
    Editor = 1,
    Owner = 2
}

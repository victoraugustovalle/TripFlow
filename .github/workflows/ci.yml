name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore TripFlow.slnx

      - name: Build
        run: dotnet build TripFlow.slnx --no-restore --configuration Release

      - name: Test
        run: dotnet test TripFlow.slnx --no-build --configuration Release --logger "console;verbosity=normal"
